node_modules/
test-results/
playwright-report/
blob-report/
playwright/.cache/
.env
.env.local
*.last-run.json
.DS_Store
probe-*.cjs
probe-*.js

# Allure (raw + generated)
allure-results/
allure-report/

# CodeGraph local index
.codegraph/

# gstack installs globally to ~/.claude/skills/gstack — never vendor it into this repo
.claude/skills/
